import { ContactSection } from "@/components/sections/ContactSection";

/** Homepage contact band — full form + professional links */
export function CtaBand() {
  return <ContactSection compact />;
}
